name: Render video

on:
  workflow_dispatch:

jobs:
  render:
    name: Render video
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Chrome dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 \
            libdbus-1-3 \
            libatk1.0-0t64 \
            libgbm-dev \
            libasound2t64 \
            libxrandr2 \
            libxkbcommon-dev \
            libxfixes3 \
            libxcomposite1 \
            libxdamage1 \
            libatk-bridge2.0-0t64 \
            libpango-1.0-0 \
            libcairo2 \
            libcups2t64

      - name: Install dependencies
        run: npm ci

      - name: Render video
        run: npx remotion render Main out/final_video.mp4 --codec=h264 --crf=18 --concurrency=4 --image-format=jpeg --jpeg-quality=95 --gl=swiftshader

      - name: Upload rendered video
        uses: actions/upload-artifact@v4
        with:
          name: predators_final_video
          path: out/final_video.mp4
          if-no-files-found: error
